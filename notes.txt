1. Redis Sorted Sets for O(log N) Ranking

Redis Sorted Sets maintain members with scores in sorted order. 
Operations are O(log N) complexity:

# From main.py line 47-54: Score Submission
current_score = redis_client.zscore(leaderboard_key, submission.user_id)
# O(1) lookup - get user's current score

if current_score is None or submission.score > current_score:
    redis_client.zadd(leaderboard_key, {submission.user_id: submission.score})
    # O(log N) insert/update - Redis maintains sorted order internally